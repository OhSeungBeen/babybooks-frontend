import { Container, Box } from "@mui/material";

const Header = () => {
  return (
    <header>
      <Box sx={{ width: "100%", bgcolor: "darkred" }}>HEADER</Box>
    </header>
  );
};

export default Header;
